function sum(soA: number, soB: number): number {
  return soA + soB;
}

console.log(sum(1, 2));

//arrow function
const sum2 = (soA: number, soB: number): number => {
  return soA + soB;
};

console.log(sum2(1, 23));
